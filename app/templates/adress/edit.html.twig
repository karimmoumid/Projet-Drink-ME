{% extends 'base.html.twig' %}

{% block title %}Modifier l'adresse !{% endblock %}

{% block body %}
    <main class="container my-5">
        <h1 class="mb-4 text-center">Modifier l'adresse !</h1>

        <div class="row justify-content-center">
            <div class="col-12 col-md-8 col-lg-6">
                {{ include('adress/_form.html.twig', {'button_label': 'Modifier'}) }}

                <div class="mt-4 text-center">
                    <a href="{{ path('app_adress_index') }}" class="btn btn-outline-secondary">
                        ← Retour vers la liste des adresses
                    </a>
                </div>

                <div class="mt-3 text-center">
                    {{ include('adress/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </main>
{% endblock %}
