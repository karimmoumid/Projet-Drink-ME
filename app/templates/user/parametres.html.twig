{% extends 'base.html.twig' %}

{% block title %}Paramètres{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">Services</h1>

        <article class="mb-5">
            <h2 class="mb-3">Comptes pro</h2>
            <ul class="list-group shadow-sm">
                <li class="list-group-item">
                    <a href="{{ path('app_user_list') }}" class="text-decoration-none d-flex justify-content-between align-items-center">
                        Liste des comptes pro
                        <i class="bi bi-box-arrow-up-right"></i>
                    </a>
                </li>
                <li class="list-group-item">
                    <a href="{{ path('app_register', {'role': 'employee'}) }}" class="text-decoration-none d-flex justify-content-between align-items-center">
                        Création d'un compte employé
                        <i class="bi bi-person-plus"></i>
                    </a>
                </li>
                <li class="list-group-item">
                    <a href="{{ path('app_register', {'role': 'admin'}) }}" class="text-decoration-none d-flex justify-content-between align-items-center">
                        Création d'un compte admin
                        <i class="bi bi-shield-lock"></i>
                    </a>
                </li>
            </ul>
        </article>

        <article>
            {# Contenu vide pour l'instant, tu peux ajouter ici #}
        </article>
    </div>
{% endblock %}
